import { FormInput } from './FormInput'
import { FormRoot } from './FormRoot'
import { FormSelect } from './FormSelect'

export const Form = {
  Root: FormRoot,
  Select: FormSelect,
  Input: FormInput,
}
